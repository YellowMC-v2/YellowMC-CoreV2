server-name: "Server"

credentials:
  database:
    url: "jdbc:mysql://localhost:3306/core_db?useSSL=false"
    user: root
    password: "password"
  redis:
    host: localhost
    port: 6379
    password: password

hikari:
  maximumPoolSize: 10
  minimumIdle: 2
  connectionTimeout: 30000
  idleTimeout: 600000
  leakDetectionThreshold: 2000
